---
id: issue-0017
status: done
type: bug
mode: AFK
source_prd: null
depends_on: []
remote:
  github: null
legacy:
  ous_id: "0014"
  ous_path: ".ous/issues/0014-keep-scry-results-within-the-command-palette-viewport.md"
  ous_state: "done"
  ous_category: "bug"
  ous_parent: "null"
  ous_tags: ["popup", "results", "pagination", "qa"]
---

# Keep Scry results within the command-palette viewport

## What to build

Scry's command-palette contents should fit within the visible popup height; result browsing should use pagination to reach additional matches.

## Acceptance examples

- [x] The command palette fits within the visible Chrome popup height without whole-popup vertical overflow for a normal result page.
- [x] Users can reach additional matching history results through pagination or equivalent page controls.
- [x] Search input, status, and footer/navigation controls remain visible and usable while moving between result pages.

## Data definition impact

Not reassessed during migration. See the migrated OUS content below for original implementation notes, decisions, and acceptance criteria.

## HtDP entry note

Migrated from OUS #0014 at `.ous/issues/0014-keep-scry-results-within-the-command-palette-viewport.md`. Original state was `done` and category was `bug`.

Use this issue as a local HtDP planning artifact. If implementation work is still required, start from the original acceptance criteria and current repository behavior rather than assuming the legacy state is still accurate.

## Verification

For code changes derived from this issue, run:

```bash
npm test
npm run check
```

Also verify any original manual or behavioral acceptance criteria preserved below.

## Blocked by

- None - can start immediately.

## HtDP iterations

- None yet.

## Out of scope

Not reassessed during migration. Preserve the original out-of-scope notes where present in the migrated OUS content below.

## Migrated OUS frontmatter

```yaml
id: "0014"
title: "Keep Scry results within the command-palette viewport"
category: bug
state: done
created: "2026-04-27T04:05:34Z"
updated: "2026-04-27T04:59:05Z"
parent: null
depends_on: []
tags: ["popup", "results", "pagination", "qa"]
```

## Migrated OUS content

## Summary

Scry's command-palette contents should fit within the visible popup height; result browsing should use pagination to reach additional matches.

## Details

## Parent issue

Reported during QA session

## What's wrong

When Scry shows enough history results, the side-panel contents extend past the screen and the panel gets a vertical scrollbar. This makes the search UI feel like an overflowing page rather than a bounded recall panel.

## What I expected

The command palette should fit the visible popup. Search results should be shown in a bounded page, with pagination or equivalent page controls to reach all available matches without making the whole popup overflow.

## Steps to reproduce

1. Load Scry as an unpacked Chrome extension in Google Chrome 147 on macOS.
2. Open the Scry command palette.
3. Search for a query that returns more history results than fit in the visible panel height.
4. Observe whether the command-palette contents stay bounded and expose pagination instead of whole-popup scrolling.

## Blocked by

None — can start immediately.

## Additional context

Scry is a history-first URL recall command palette. The desired browsing model is a fixed-height popup experience where the search input, status, result page, and controls remain usable while paging through matches.

## Acceptance Criteria

- [x] The command palette fits within the visible Chrome popup height without whole-popup vertical overflow for a normal result page.
- [x] Users can reach additional matching history results through pagination or equivalent page controls.
- [x] Search input, status, and footer/navigation controls remain visible and usable while moving between result pages.

## Activity

### 2026-04-27T04:05:34Z — AI

> *This was generated by AI during local issue triage.*

Filed from QA report that Scry's side-panel contents overflow vertically and should use pagination instead of a full-panel scrollbar.

### 2026-04-27T04:28:36Z — AI

Implemented fixed-height side-panel layout, paged result rendering, page controls, and h/l page navigation. Automated panel tests cover paged results and page movement; Chrome manual viewport smoke testing remains.

### 2026-04-27T04:59:05Z — AI

Carried the bounded result layout forward into the popup command palette. The popup uses a fixed-size command-palette viewport with paged results.
