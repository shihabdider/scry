# Agent Notes

htdp.mode: autonomous
htdp.transparent: true

## Project conventions

- Keep Scry a local-only Chrome MV3 popup command palette.
- Run `npm test` and `npm run check` after implementation changes.
- Avoid external network calls, host permissions, content scripts, and options pages unless an issue explicitly changes the product boundary.
